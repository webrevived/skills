interface:
  display_name: "Codex Loop"
  short_description: "Review staged changes and verify fixes with Codex"
  default_prompt: "Use $codex-loop to review the staged changes and verify the fixes."

policy:
  allow_implicit_invocation: false

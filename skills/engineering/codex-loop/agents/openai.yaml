interface:
  display_name: "Codex Loop"
  short_description: "Multi-agent staged review with bounded fix verification"
  default_prompt: "Use $codex-loop with Astra at the default high level to review staged changes, fix valid findings, and verify the fixes."

policy:
  allow_implicit_invocation: false
